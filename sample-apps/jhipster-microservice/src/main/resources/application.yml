server:
  port: 8080
spring:
  application:
    name: jhipster-microservice
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
